package Arrays_Practice;

public class Bin_Int_Int_Bin {
    public static void main(String[] args) {
        // Binary -> Integer
        // Binary -> Integer
        // Binary -> Integer
//        int num = 1111;
//        double res=0;
//        while (num>0) {
//            int t = (int) (Math.log(num) / Math.log(10)); // Find the highest power of 2  3
//            res += Math.pow(2, t); // Add a '1' at the correct position   1000
//            num -= Math.pow(10, t); // Subtract that power of 2
//        }
//
//        System.out.println((int)res);


        // Integer -> Binary
//        int num = 15;
//        double res=0;
//        while (num>0) {
//            int t = (int) (Math.log(num) / Math.log(2)); // Find the highest power of 2  3
//            res += Math.pow(10, t); // Add a '1' at the correct position   1000
//            num -= Math.pow(2, t); // Subtract that power of 2
//        }
//
//        System.out.println((int)res);
    }
}
